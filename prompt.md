# chern_capital 项目交接与持续规则

> 给后续接手本项目的 Codex：开始修改前，请先阅读本文件，再查看当前页面与相关源文件。这个站点不是一次性演示稿，而是在逐步累积的个人交易工作台；新增功能应建立在已有内容之上。

## 项目目标

`chern_capital` 是一个个人交易网站，当前主要承载六类内容：

1. **脚本筛选**：说明 VCP 筛选与图表复核流程，保留具体规则和运行方式。
2. **实盘记录**：展示真实交易的买卖位置、日线图、持仓天数和已实现盈亏。
3. **结构库**：按 A 股（A）与虚拟货币（B）沉淀图形结构、标签和少量文字说明。
4. **交易日志**：将每日观察锚定到中证 1000 日线对应点位，保留市场空间感与当日判断。
5. **记忆库**：把可复盘的行情样本、交易员画像和关键事件锚定到价格路径上。
6. **交易阅读**：用空间记忆与关联路径的方式呈现阅读摘录和心得，第一本为《缠论》。

网站视觉应保持克制、深色、交易工作台风格；内容重点是可阅读、可复核、可继续扩展，而不是营销式首页。

## 当前页面结构

| 文件 | 用途 | 必须保留的内容 |
| --- | --- | --- |
| `index.html` | 首页与交易实战总览 | “脚本筛选”“实盘记录”“结构库”“交易日志”并列入口；“记忆库”一级板块与 BTC 交易员关键点；VCP 的详细流程、规则、输出说明；阅读入口 |
| `assets/structure/` | 结构库图片资产 | 结构库图像按板块和编号保存；当前有虚拟货币 B 板块的 `b-1-hype.png` |
| `reading.html` | 阅读详情页 | 《缠论》记忆宫殿、空间化视觉、关联路线以及完整摘录内容 |
| `position.html` | 实盘详情页 | 安洁科技已结束交易卡片、日线图、买卖位置、已实现盈亏和日线更新按钮 |
| `position-605389.html` | 实盘详情页 | 长龄液压持仓中交易卡片、日线图、建仓位置、持仓天数、浮动盈亏和行情更新按钮 |
| `position.js` / `position-605389.js` | 实盘前端逻辑 | 初始行情快照、K 线绘制、买卖/建仓标记、盈亏计算、进入页自动更新与手动更新 |
| `functions/api/position.js` / `functions/api/position-605389.js` | Cloudflare Pages Function | 服务器侧转发新浪财经行情，供实盘页面更新日线数据 |
| `styles.css` / `script.js` | 视觉和公共交互 | 整体样式、背景连接点动画以及响应式布局 |

## 当前事实与数据口径

- 实盘目前有两笔记录：安洁科技 `002635` 已结束；长龄液压 `605389` 持仓中。
- 建仓日期：`2026-05-12`。
- 成交价格：`16.61` 元/股。
- 数量：`500` 股。
- 买入成交金额：`8,305.00` 元。
- 用户给出的投入金额：`8,310.00` 元；页面盈亏按此投入金额计算。
- 卖出日期：`2026-05-28`。
- 卖出价格：`17.57` 元/股。
- 卖出数量：`500` 股。
- 卖出成交金额：`8,785.00` 元。
- 卖出手续费：`5.00` 元。
- 净回收：`8,780.00` 元。
- 已实现盈亏：`+470.00` 元，收益率约 `+5.66%`。
- 此页是个人持仓过程记录，不构成投资建议。
- 已结束交易的盈亏按买卖记录固定，不再随最新价波动。
- 第二笔实盘为长龄液压 `605389`，状态为持仓中；`2026-06-01` 以 `85.00` 元/股买入 `100` 股，买入成交金额 `8,500.00` 元，手续费暂未记录，不要自行补成固定费用。
- 长龄液压页面的浮动盈亏按最新价 × `100` 股减 `8,500.00` 元计算；后续若用户提供手续费或卖出记录，再固定成本口径和已实现盈亏。
- 行情刷新数据源为新浪财经；进入 Cloudflare 实盘详情页时自动请求一次，用于补全日线图或刷新持仓价，访问者也可再次点击更新按钮。请求均通过 Cloudflare Function 转发；不要把依赖 `Referer` 的新浪即时行情请求直接改成浏览器跨域请求。
- GitHub Pages 是静态版本，可展示页面和快照，但不能运行 `/api/position*`；实时刷新以 Cloudflare Pages 版本为准。

## 交易日志口径

- 首页新增“交易日志”板块，属于交易实战而不是阅读。
- 当前第一条日志日期为 `2026-05-27`，锚定中证 1000 `000852` 日线收盘点 `8,546.51`。
- 日志原文为：安洁科技跑输科瑞技术，当前情绪比较加速。
- 日志板块应保留“市场路径 + 当日记录”的空间关系；新增日志时优先追加到同一坐标体系，而不是改成普通列表。

## 结构库口径

- 结构库和实盘交易并列，属于交易实战下的独立模块。
- 结构库主要承载图片和文字内容，不记录真实买卖，不替代实盘详情页。
- 当前分为 A 股（A）与虚拟货币（B）两个大板块。
- 当前第一条结构为虚拟货币板块 `B-1`，标签为 `hype`、`涨`、`跌`，文字说明暂空。
- 结构库图片资产放在 `assets/structure/`；后续若替换原始截图，优先保持引用路径稳定。

## 记忆库口径

- 记忆库与“交易实战”“阅读”并列，是首页一级板块，不塞进实盘或阅读子项中。
- 当前唯一子版块为 BTC，用类似 TradingView 的静态价格路径承载关键交易窗口。
- BTC 记忆图的关键点来自 `资料/trader_strategy_prompt_resource.md`：这是交易员资产曲线与 BTC 价格对齐后的分析资料。
- 页面必须清楚区分“数据事实”和“策略推断”：资产曲线来自 SVG 反算抽样，不是 Binance 原始逐日账本；关键点不是逐笔成交记录。
- `资料/trader_strategy_prompt_resource.md` 是本地资料源，当前不要提交或部署到 GitHub / Cloudflare，除非用户明确要求公开。

## VCP 脚本关系

这一点曾经被页面摘要模糊过，后续不要再混写：

- `vcp_screener.py` 负责扫描股票池并执行筛选规则，输出 `vcp_result_YYYYMMDD.csv`。
- `vcp_plot.py` 读取最新筛选结果，拉取前复权日线，生成可交互复盘页面 `vcp_charts_YYYYMMDD_HHMM.html`。
- 两者共同构成“先筛，再看图”的流程；`vcp_plot.py` 不是完整筛选规则本身。

当前需要在网站中保留的筛选条件：

1. 股票池限定为代码以 `60` 或 `00` 开头的沪深主板股票，排除 ST 与退市标记样本。
2. 周线满足 `Close > MA10 > MA30`，且周 MA10、MA30 相比 4 周前均向上。
3. 收盘价高于日线 MA50，距 52 周高点回撤在 `(3%, 25%]` 内，并高于 52 周低点的 `1.3` 倍。
4. 近 5 日平均成交量低于近 20 日均量的 `80%`：`vol5 / vol20 < 0.8`。
5. 近 5 日平均振幅低于近 20 日平均振幅的 `80%`：`amp5 / amp20 < 0.8`。
6. 最近 5 日中，后 3 日平均振幅低于前 2 日，表达整理尾端继续变紧。

## 内容与设计规则

以下规则优先级很高。除非用户明确要求改变，否则每次版本更新都应遵守。

### 内容保留

1. **新增功能不能以大幅压缩既有内容为代价。** 尤其不能因为新增卡片、模块或导航入口，就把已经确认过的说明压成一句摘要。
2. 修改某个板块前，先清点其已有标题、规则、数据和可访问入口；改完后检查这些内容仍存在，或被明确迁移到可点击到达的详情页。
3. 如果因层级整理需要将正文移到详情页，首页必须保留清晰入口与足够语境，且详情页应承接原有信息量。
4. 用户提供的原始摘录、持仓记录、脚本规则属于核心资料；不应自行删改、泛化或替换为空泛文案。
5. 每次产生新的结构调整或关键内容变更时，同步更新本文件的版本记录和规则说明。

### 信息层级

1. 首页以“交易实战”“记忆库”“阅读”为第一层级，避免重新变成内容混杂的长列表。
2. “脚本筛选”和“实盘”属于交易实战下的并列模块；实盘新增不能挤掉脚本说明。
3. “结构库”属于交易实战下的并列模块，应和实盘交易区分开：实盘记录买卖，结构库沉淀可复用图形。
4. “交易日志”也属于交易实战下的并列模块，应靠近实盘记录，帮助从市场位置回看个股强弱与情绪变化。
5. VCP 的流程和六条规则应继续能在首页阅读；它们不是装饰性简介。
6. “记忆库”用于沉淀行情样本、交易员画像和关键事件坐标；新增样本应按子版块追加，不覆盖 BTC 第一条。
7. “阅读”在首页保持入口性质，具体摘录与记忆宫殿体验放在 `reading.html`。
8. 未来新增其他实盘交易、结构样本、交易日志、记忆样本或其他书籍时，延续同层新增记录/书架项的结构，不覆盖现有第一条内容。

### 视觉与交互

1. 延续当前深色、克制、偏交易终端/工作台的风格，不改造成营销落地页。
2. 阅读宫殿必须包含空间感、视觉符号与关联路径，不能退化为只有文字说明的列表。
3. 实盘详情应以日线图为主要叙事载体，保留建仓线、成交记录、持仓指标与盈亏说明。
4. 所有新增界面必须检查桌面与手机布局，避免文字遮挡、日期刻度重叠或横向滚动。
5. 数据刷新等操作必须给访问者明确状态反馈；失败时保留已有快照并说明原因。
6. 实盘详情页进入 Cloudflare 版本时应自动刷新一次行情或日线数据，并保留手动刷新按钮；GitHub Pages 静态版本不自动发起必定失败的接口请求。
7. 已结束交易应展示买入点、卖出点、净回收和已实现盈亏；不要继续把它写成“当前持仓”或“浮动盈亏”。

### 数据与发布安全

1. 不将行情展示表述为投资建议，不替用户生成买卖结论。
2. 用户的真实交易数据可以按其明确提供的内容展示；添加新交易前核对日期、数量、成交价与成本口径。
3. 新浪即时行情当前通过 Cloudflare Function 请求，并需要服务端设置来源头；修改 API 前先验证线上数据请求是否仍可用。
4. 不把密钥、令牌、登录状态或本地部署缓存提交到仓库。
5. `vcp/`、`read/` 与 `.wrangler/` 当前被 `.gitignore` 排除；除非用户明确同意，不上传原始脚本、原始阅读文件或部署缓存。

## 本地素材与换电脑提醒

以下源材料目前存在于原电脑工作区，但不在 Git 仓库中：

| 本地目录/文件 | 内容 | 当前状态 |
| --- | --- | --- |
| `vcp/vcp_plot.py` | VCP 候选图表生成脚本 | 被 Git 忽略，未上传 |
| `vcp/vcp_screener.py` | VCP 筛选规则脚本 | 被 Git 忽略，未上传 |
| `vcp/vcp_result_*.csv` / `vcp_charts_*.html` | 筛选结果与生成页面 | 被 Git 忽略，未上传 |
| `read/chan.md` | 《缠论》摘录与心得原文 | 被 Git 忽略，未上传 |
| `资料/trader_strategy_prompt_resource.md` | 交易员资产曲线与 BTC 策略推断资料 | 本地资料源，未上传；当前只用于生成首页记忆库内容 |

如果要在另一台电脑继续编辑脚本原文或阅读原文，需要单独同步这些本地文件；仅克隆 GitHub 仓库只能获得已经写入网页的呈现版本。

## 部署现状

- GitHub 仓库：`https://github.com/chensher/chern-capital`
- GitHub Pages：`https://chensher.github.io/chern-capital/`
- Cloudflare Pages：`https://chern-capital.pages.dev/`
- GitHub Pages 用于静态页面发布。
- Cloudflare Pages 同时承载静态页面和 `functions/api/position*.js` 实时行情接口。

发布新版本时：

1. 先在本地检查页面内容和响应式显示。
2. 提交并推送到 GitHub `main` 分支，让 GitHub Pages 更新。
3. 若改动影响 Cloudflare 页面或 Function，同步部署到 Cloudflare Pages。
4. 在线核对主要页面，并在涉及实盘时测试手动刷新接口。

## 版本记录

| 日期 | Git 提交 | 变更内容 | 后续需要记住的决定 |
| --- | --- | --- | --- |
| 2026-05-22 | `11e031d` | 创建零构建个人网站初版 | 使用免费静态托管，站点标题为 `chern_capital` |
| 2026-05-22 | `3427006` | 用 VCP 脚本说明替换初始内容 | 网站开始承载实际交易工具说明，规则和流程属于核心内容 |
| 2026-05-22 | `e6e9e8e` | 增加《缠论》阅读记忆宫殿 | 阅读将作为长期可扩展的书架体系 |
| 2026-05-22 | `227d3f8` | 将阅读细节拆分至独立详情页 | 首页应保持层级清晰，阅读细节进入独立页面 |
| 2026-05-22 | `e6ed88f` | 增强记忆宫殿的空间视觉 | 阅读体验必须保留空间、图像与关联性，不只堆文字 |
| 2026-05-25 | `ab5f2c8` | 增加《实盘》模块和安洁科技日线详情页 | 实盘与脚本筛选并列；Cloudflare 提供手动刷新行情接口 |
| 2026-05-25 | `2a1954a` | 恢复被压缩的 VCP 完整说明 | 新增模块不可牺牲既有内容，VCP 规则必须保持可阅读 |
| 2026-05-26 | `3846972` | 建立 `prompt.md` 作为项目交接与规则记录 | 后续关键变更应同步补充本文件 |
| 2026-05-26 | `6bd53a6` | 实盘详情页进入时自动获取新浪行情 | 自动刷新只在可用实时接口的部署上触发，并继续保留手动刷新 |
| 2026-05-28 | `1cfd186` | 将安洁科技实盘更新为已结束交易 | 盈亏按卖出记录固定，日线刷新只用于补全复盘图 |
| 2026-05-31 | 本次更新 | 新增交易日志板块，并将 2026-05-27 记录锚定到中证 1000 日线点位 | 交易日志属于交易实战，应保留市场路径与当日记录的空间关系 |
| 2026-06-01 | 本次更新 | 新增长龄液压 `605389` 第二笔实盘持仓与独立详情页 | 新增实盘应追加清单和详情页，不覆盖安洁科技已结束复盘；未提供手续费时不要虚构费用 |
| 2026-06-09 | 本次更新 | 新增结构库模块，并在虚拟货币 B 板块加入 `B-1` 图形结构 | 结构库与实盘交易并列；结构样本以图片和标签为主，文字可暂空 |
| 2026-06-15 | 本次更新 | 新增“记忆库”一级板块，并加入 BTC 交易员关键点复盘图 | 记忆库与交易实战、阅读并列；BTC 关键点来自本地资料源，页面要保留推断边界 |

## 后续 Codex 开始工作前的检查清单

1. 阅读本文件，并查看 `git status` 与最近提交，确认是否有未完成的用户改动。
2. 定位本次请求影响到的现有页面和本地素材，先理解已有内容再编辑。
3. 判断新增内容应属于“脚本筛选”“实盘”“结构库”“交易日志”“记忆库”还是“阅读”，保持层级一致。
4. 编辑后核对原有核心正文没有被无意删除或缩成摘要。
5. 对用户可见页面检查桌面与手机显示；对行情功能检查 Cloudflare 接口和失败状态。
6. 发布后验证 GitHub Pages 与 Cloudflare Pages 的实际在线页面。
7. 若形成新的长期约束、功能或页面结构，更新本文件的版本记录。
